# pnpm's default symlinked layout breaks Metro's resolver for packages
# whose `main` field uses subpath exports (expo-router/entry, etc).
# Hoisted layout = flat npm-style node_modules; Metro is happy with it
# and Expo officially supports this combination.
node-linker=hoisted
